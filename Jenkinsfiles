node ('workstation') {
    try {
        stage ("one") {
            echo "stage one"
        }
        stage ("two") {
            echo "stage two"
            sh exit 1
        }
    }
    catch (e) {
        stage ("three") {
            echo "i wil run on failures"
        }
    }
}