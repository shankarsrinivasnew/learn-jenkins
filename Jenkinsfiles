node ('workstation') {
    try {
        stage ("one") {
            echo "stage one"
            sh exit 1
        }
        stage ("two") {
            echo "stage two"
        }
    }
    catch (e) {
        stage ("three") {
            echo "i wil run on failures"
        }
    }
}